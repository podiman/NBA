import React from 'react';

export const SearchBar = () => {
    return (
        <div className="search-bar">
            <input className="search-input" type="text" placeholder="Search Player" />
        </div>
    );
}